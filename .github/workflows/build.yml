# Standalone builder: build a signed/notarized macOS DMG of the latest UPSTREAM
# stable release, but WITH the boxmot tracking extra bundled and the in-app
# updater pointed at THIS repo (so installed apps auto-update from here).
#
# This repo is NOT a fork. It holds no AutoPTZ source at rest — only this
# workflow. Each run checks out upstream (AutoPTZ/autoptz) at the release tag and
# applies the two build-time tweaks:
#   * install with --with-tracking  -> boxmot bundled into the .app
#   * sed DEFAULT_REPO -> this repo  -> installed app auto-updates from here
# Because upstream source is fetched fresh each build and never committed here,
# there is nothing to rebase and no merge conflicts, ever.
#
# Result: whenever upstream cuts a newer stable release, this repo rebuilds a
# boxmot DMG, publishes it here, and your installed app auto-updates to it.
name: AutoPTZ BoxMOT build

on:
  schedule:
    - cron: "0 9 * * *" # daily 09:00 UTC
  workflow_dispatch:
    inputs:
      force:
        description: "Build even if this repo already ships the latest upstream tag."
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: autoptz-boxmot-build
  cancel-in-progress: false

env:
  UPSTREAM_REPO: AutoPTZ/autoptz

jobs:
  # Decide whether upstream has a newer stable release than this repo already ships.
  check:
    name: Check for new upstream release
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.decide.outputs.tag }}
      should_build: ${{ steps.decide.outputs.should_build }}
    steps:
      - id: decide
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          # /releases/latest returns the newest NON-prerelease, non-draft release,
          # which matches the app's stable-only default update channel.
          up="$(gh api "repos/${UPSTREAM_REPO}/releases/latest" -q .tag_name)"
          mine="$(gh api "repos/${GITHUB_REPOSITORY}/releases/latest" -q .tag_name 2>/dev/null || echo "")"
          echo "upstream latest: ${up:-<none>}"
          echo "this repo latest: ${mine:-<none>}"
          force="${{ github.event.inputs.force }}"
          if [ -z "${up}" ]; then
            echo "No upstream stable release found; nothing to build."
            echo "should_build=false" >> "$GITHUB_OUTPUT"
          elif [ "${up}" != "${mine}" ] || [ "${force}" = "true" ]; then
            echo "should_build=true" >> "$GITHUB_OUTPUT"
          else
            echo "This repo already ships ${mine}; nothing to do."
            echo "should_build=false" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=${up}" >> "$GITHUB_OUTPUT"

  # Build the signed/notarized DMG for each macOS architecture.
  build:
    name: Build ${{ matrix.arch }} DMG
    needs: check
    if: needs.check.outputs.should_build == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            runner: macos-14
          - arch: x86_64
            runner: macos-26-intel
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    steps:
      # Check out the UPSTREAM source at the exact release tag we are packaging.
      - uses: actions/checkout@v4
        with:
          repository: ${{ env.UPSTREAM_REPO }}
          ref: ${{ needs.check.outputs.tag }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Build tweak #1: point the in-app updater at THIS repo so the installed app
      # auto-updates from here (with boxmot) instead of upstream. Applied at build
      # time only — the checked-out upstream source is otherwise untouched.
      - name: Point updater at this repo
        run: |
          set -euo pipefail
          python - "$GITHUB_REPOSITORY" <<'PY'
          import pathlib, re, sys
          repo = sys.argv[1]
          p = pathlib.Path("autoptz/update/checker.py")
          s = p.read_text()
          s2 = re.sub(r'DEFAULT_REPO = "[^"]+"', f'DEFAULT_REPO = "{repo}"', s, count=1)
          assert s2 != s, "DEFAULT_REPO line not found — upstream layout changed"
          p.write_text(s2)
          print(f"DEFAULT_REPO -> {repo}")
          PY

      # Import the Developer ID cert into a temporary keychain so build_macos.sh
      # can codesign + notarize.
      - name: Import Developer ID certificate
        env:
          CERT_B64: ${{ secrets.MACOS_CERTIFICATE_P12_BASE64 }}
          CERT_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          SIGN_IDENTITY: ${{ secrets.MACOS_SIGN_IDENTITY }}
        run: |
          set -euo pipefail
          KEYCHAIN="$RUNNER_TEMP/signing.keychain-db"
          KEYCHAIN_PW="$(uuidgen)"
          printf '%s' "$CERT_B64" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security create-keychain -p "$KEYCHAIN_PW" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PW" "$KEYCHAIN"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN" -P "$CERT_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PW" "$KEYCHAIN" >/dev/null
          security list-keychains -d user -s "$KEYCHAIN" \
            $(security list-keychains -d user | sed 's/["[:space:]]//g')
          rm -f "$RUNNER_TEMP/cert.p12"
          security find-identity -v -p codesigning "$KEYCHAIN"
          if ! security find-identity -v -p codesigning "$KEYCHAIN" \
              | grep -F "Developer ID Application:" >/dev/null; then
            echo "::error::Imported .p12 has no Developer ID Application identity."
            exit 1
          fi

      # Build tweak #2: install WITH the boxmot tracking extra into the venv that
      # build_macos.sh reuses (SKIP_INSTALL=1 keeps the script from reinstalling
      # torch-free). PyInstaller then bundles boxmot into the .app.
      - name: Install dependencies (with boxmot)
        run: |
          set -euo pipefail
          python -m venv .venv
          .venv/bin/python tools/install.py \
            --upgrade-pip --packaging --with-tracking --editable --accelerator cpu

      - name: Build signed + notarized DMG
        env:
          SKIP_INSTALL: "1"
          MAKE_DMG: "1"
          MACOS_SIGN_IDENTITY: ${{ secrets.MACOS_SIGN_IDENTITY }}
          MACOS_NOTARY_APPLE_ID: ${{ secrets.MACOS_NOTARY_APPLE_ID }}
          MACOS_NOTARY_TEAM_ID: ${{ secrets.MACOS_NOTARY_TEAM_ID }}
          MACOS_NOTARY_PASSWORD: ${{ secrets.MACOS_NOTARY_PASSWORD }}
          MACOS_SIGN_REQUIRED: "1"
        run: bash packaging/build_macos.sh

      - uses: actions/upload-artifact@v4
        with:
          name: dmg-${{ matrix.arch }}
          path: dist/*.dmg
          if-no-files-found: error
          compression-level: 0

  # Publish both DMGs (plus a SHA256SUMS manifest the in-app updater verifies)
  # as a release on THIS repo, tagged to match the upstream release.
  publish:
    name: Publish release ${{ needs.check.outputs.tag }}
    needs: [check, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Assemble assets + SHA256SUMS
        run: |
          set -euo pipefail
          mkdir -p out
          find artifacts -type f -name '*.dmg' -exec cp {} out/ \;
          cd out
          sha256sum ./*.dmg | sed 's# \./#  #' > SHA256SUMS
          echo "--- SHA256SUMS ---"
          cat SHA256SUMS
      - name: Publish / update release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.check.outputs.tag }}
        run: |
          set -euo pipefail
          notes="Automated build of upstream ${TAG} with the **boxmot** tracking extra bundled (BoT-SORT/DeepOCSORT/ByteTrack + OSNet ReID). The in-app updater on these builds tracks this repo.

          Upstream source: https://github.com/${UPSTREAM_REPO}/releases/tag/${TAG}"
          if gh release view "${TAG}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
            gh release upload "${TAG}" out/* --repo "${GITHUB_REPOSITORY}" --clobber
          else
            gh release create "${TAG}" out/* \
              --repo "${GITHUB_REPOSITORY}" \
              --title "AutoPTZ ${TAG} (BoxMOT build)" \
              --notes "${notes}" \
              --latest
          fi
